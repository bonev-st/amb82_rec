# Copy to server/.env and fill in the generated recorder password.
# server/.env is gitignored — never commit the real file.
#
# This value is produced by server/certs/generate-passwords.sh on the
# host where the stack will run. Copy the "recorder" password that the
# script prints.

MQTT_RECORDER_PASSWORD=replace-me
